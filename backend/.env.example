PORT=3000
FRONTEND_ORIGIN=http://localhost:5500
JWT_SECRET=replace-with-a-long-random-secret
# Optional: store the JSON data files somewhere else, mainly used by the automated tests.
# DATA_DIR=./data
